services:
  - type: web
    name: kci-press-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: KCI_SERVICE_KEY
        sync: false
